{
  "name": "herenow-discover",
  "version": "1.0.0",
  "description": "Discover and catalog sites published on here.now",
  "type": "module",
  "bin": {
    "herenow-discover": "./src/index.ts"
  },
  "scripts": {
    "discover": "bun run src/index.ts discover",
    "validate": "bun run src/index.ts validate",
    "sync": "bun run src/index.ts sync",
    "stats": "bun run src/index.ts stats"
  },
  "license": "CC0-1.0",
  "devDependencies": {
    "@types/bun": "^1.3.0"
  }
}
